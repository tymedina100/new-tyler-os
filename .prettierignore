.next
node_modules
pnpm-lock.yaml
coverage
playwright-report
test-results
.playwright

# Generated by drizzle-kit and owned by it. The SQL was already ignored; the
# meta snapshots need to be too, because `pnpm format` was rewriting files that
# drizzle-kit compares byte-for-byte against the next generated snapshot.
drizzle
